---
import HeaderLink from './HeaderLink.astro';
---

<header class="site-header">
	<a class="skip-link" href="#main-content">本文へ移動</a>
	<nav class="shell" aria-label="メインナビゲーション">
		<a class="brand" href="/" aria-label="玉城祐輔 ホーム">
			<span class="brand-mark" aria-hidden="true">
				<img src="/images/tamashiro-yusuke-logo.png" alt="" width="512" height="512" decoding="async" />
			</span>
			<strong>玉城祐輔</strong>
		</a>
		<div class="internal-links">
			<HeaderLink href="/blog">読む</HeaderLink>
			<HeaderLink href="/toolbox">道具箱</HeaderLink>
			<HeaderLink href="/about">玉城について</HeaderLink>
		</div>
	</nav>
</header>

<style>
	.site-header {
		position: sticky;
		top: 0;
		z-index: 10;
		border-bottom: 1px solid var(--site-line, var(--line));
		background: var(--header-bg, color-mix(in srgb, var(--paper) 92%, transparent));
		backdrop-filter: blur(16px);
	}

	nav {
		display: flex;
		align-items: center;
		justify-content: space-between;
		min-height: 72px;
		gap: 1.25rem;
	}

	.brand {
		display: inline-flex;
		align-items: center;
		gap: 0.8rem;
		text-decoration: none;
	}

	.brand-mark {
		width: 2.25rem;
		height: 2.25rem;
		flex: 0 0 auto;
		padding: 0.14rem;
		overflow: hidden;
		border-radius: 0.7rem;
		background: #f4f0e7;
	}

	.brand-mark img {
		display: block;
		width: 100%;
		height: 100%;
		object-fit: contain;
	}

	.brand strong {
		font-size: 0.94rem;
		white-space: nowrap;
	}

	.internal-links {
		display: flex;
		align-items: center;
		gap: clamp(0.7rem, 2.5vw, 2rem);
	}

	.skip-link {
		position: absolute;
		top: -100px;
		left: 1rem;
		z-index: 20;
		padding: 0.7rem 1rem;
		background: var(--ink);
		color: var(--white);
	}

	.skip-link:focus {
		top: 0.7rem;
	}

	@media (max-width: 620px) {
		nav { min-height: 64px; gap: 0.7rem; }
		.brand { gap: 0.5rem; }
		.brand-mark { width: 2rem; height: 2rem; }
		.internal-links { gap: 0.65rem; }
		.internal-links :global(a) { font-size: 0.75rem; }
	}

	@media (max-width: 390px) {
		.brand strong { position: absolute; width: 1px; height: 1px; overflow: hidden; clip: rect(0, 0, 0, 0); }
	}
</style>

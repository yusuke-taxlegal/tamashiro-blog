---
import BaseHead from './BaseHead.astro';
import Footer from './Footer.astro';
import Header from './Header.astro';

interface Props {
	view?: 'index' | 'work' | 'home';
}

const { view = 'index' } = Astro.props;

const tools = [
	{
		id: 'hidock-p1',
		group: 'work',
		category: '会議・相談の記録',
		channel: 'メーカー紹介',
		title: 'HiDock P1',
		statement: '目の前の相手との会話に、集中する。',
		lead: '録音、文字起こし、要約まで。会議後に長い録音を聞き直す時間を減らすために使っているAIレコーダーです。',
		details: [
			['使う場面', '商工会の相談窓口、巡回相談、オンライン会議、セミナー。'],
			['良かった点', '報告書や議事録の下書きを早く作り、会話中はメモより相手に集中できます。'],
			['購入前の注意', '録音の同意と、AIが作った要約を人が確認する運用が必要です。'],
		],
		url: 'https://jp.hidock.com/pages/referral-program?ref=uzxdtbsg',
		image: 'https://jp.hidock.com/cdn/shop/files/HiDock_P1_Space_Gray_Front_Image.jpg?v=1779855196&width=1200',
		imageAlt: 'HiDock P1 AIボイスレコーダー スペースグレーの正面',
		button: '公式サイトで10%OFF',
		note: '紹介リンクです。購入者はHiDock全製品10%OFFになります。',
		tone: 'coral',
		featured: true,
	},
	{
		id: 'nihontsushin-sim',
		group: 'work',
		category: '通信・モバイル',
		channel: 'Amazon',
		title: '日本通信SIM 合理的みんなのプラン',
		statement: '月額1,390円で20GB。外の仕事も、これで十分。',
		lead: '外でパソコンを使うときは、スマホのテザリングを利用しています。メール、LINE、チャット、クラウド上のデータ利用、ウェブ会議まで、私の使い方ではこの20GBで間に合っています。',
		details: [
			['使う場面', '外出先でパソコンを使うときのテザリングと、普段のスマホ通信。'],
			['良かった点', '外でWi-Fiを探すことがほとんどなくなり、月額料金も抑えられています。'],
			['通話', '1回5分までの国内通話かけ放題、または月70分までの無料通話のどちらかを選べます。'],
			['契約前の注意', 'ウェブ会議や動画はデータを多く使い、通信速度は場所や時間帯などで変わります。自分の利用量と対応エリアを確認してください。'],
		],
		url: 'https://amzn.to/4zgLclg',
		officialUrl: 'https://www.nihontsushin.com/plan/planminna.html',
		image: 'https://www.nihontsushin.com/plan/images/starterpack_pkg.jpg',
		imageAlt: '日本通信SIM スターターパック NT-ST2-P',
		button: 'Amazonでスターターパックを見る',
		note: 'リンク先は申込用スターターパックです。SIMカードは同梱されていません。価格、申込期限、対象プランを確認し、公式サイトから直接申し込む場合の初期手数料3,300円とも比較してください。',
		tone: 'olive',
		featured: true,
	},
	{
		id: 'switchbot-lock',
		group: 'home',
		category: '玄関・スマートホーム',
		channel: 'Amazon',
		title: 'SwitchBot スマートロック Pro＋指紋認証パッド',
		statement: '鍵を出す動作と、閉め忘れを減らす。',
		lead: '家族それぞれに合う解錠方法を選びやすい、後付け型のスマートロックです。',
		details: [
			['使う場面', '毎日の玄関。指紋、暗証番号、交通系ICカードなどから解錠方法を選べます。'],
			['良かった点', '鍵を探す手間が減り、オートロックで閉め忘れも減らせます。'],
			['購入前の注意', 'ドアとサムターンの形状、取り付け位置のスペースを確認してください。'],
		],
		url: 'https://amzn.to/4hszTQF',
		image: 'https://m.media-amazon.com/images/I/61lQHV7vkFL._AC_SL1500_.jpg',
		imageAlt: 'SwitchBot スマートロック Proと指紋認証パッド',
		button: 'Amazonで商品を見る',
		note: '価格や在庫はAmazonの商品ページでご確認ください。',
		tone: 'olive',
	},
	{
		id: 'sparkling-water',
		group: 'home',
		category: '暮らし・飲み物',
		channel: 'Amazon',
		title: '富士山の強炭酸水 ラベルレス 500ml×24本',
		statement: '重いケースを、店から運ばなくていい。',
		lead: '約2カ月に1回購入している、わが家の定番です。機能よりも、箱で家まで届くことが助かっています。',
		details: [
			['使う場面', '日々の飲み物として、ケースで買い置きしています。'],
			['良かった点', '重い炭酸水を自分で運ぶ負担と、ラベルをはがす手間を減らせます。'],
			['購入前の注意', '容量、本数、価格、配送条件は購入時に確認してください。'],
		],
		url: 'https://www.amazon.co.jp/dp/B09LCRNQT4?tag=tamashirotool-22',
		image: 'https://m.media-amazon.com/images/I/61bGltUy7IL._AC_SL1000_.jpg',
		imageAlt: 'アイリスオーヤマ 富士山の強炭酸水 ラベルレス 500ml 24本',
		button: 'Amazonで商品を見る',
		note: '容量、本数、価格、配送条件はAmazonの商品ページでご確認ください。',
		tone: 'paper',
	},
	{
		id: 'soy-milk',
		group: 'home',
		category: '暮らし・飲み物',
		channel: 'Amazon',
		title: 'マルサン 調製豆乳 カロリー45%オフ 1L×6本',
		statement: '家にある安心を、まとめて買っておく。',
		lead: '2〜3カ月に1回購入しています。未開封のまま買い置きしやすく、飲みたいときに家にあるのが助かります。',
		details: [
			['使う場面', '健康を意識して飲み物を選びたいときの日常の選択肢です。'],
			['良かった点', '6本まとめて届き、未開封なら常温で保管できます。'],
			['購入前の注意', '開封後は冷蔵し、商品表示に従って早めに飲み切ります。'],
		],
		url: 'https://amzn.to/4bDXsSX',
		image: 'https://m.media-amazon.com/images/I/71ic+ExuwhL._AC_SL1400_.jpg',
		imageAlt: 'マルサン 調製豆乳 カロリー45%オフ 1L 6本',
		button: 'Amazonで商品を見る',
		note: '賞味期限、保存方法、内容量、原材料、価格は商品ページと届いた商品の表示をご確認ください。',
		tone: 'paper',
	},
	{
		id: 'sweet-chili-sauce',
		group: 'home',
		category: '食卓・調味料',
		channel: 'Amazon',
		title: 'トマトコーポレーション 業務用スイートチリソース 750g×4個',
		statement: 'タコスの日に、足りなくならない。',
		lead: '「バーバの作るタコス」が大好きな玉城家。市販サイズではすぐになくなるため、Amazonで業務用サイズをまとめ買いしています。',
		details: [
			['使う場面', 'バーバがタコスを作ってくれる日の、玉城家の定番ソースです。'],
			['良かった点', '750gの業務用サイズを家に置いておけるので、家族でたっぷり使っても買い足す頻度を減らせます。'],
			['購入前の注意', 'リンク先は750g×4個セットです。使い切れる量か確認し、原材料、賞味期限、開封後の保存方法は届いた商品の表示に従ってください。'],
		],
		url: 'https://amzn.to/4g0uj6X',
		image: 'https://www.tomato-corporation.co.jp/data/products/6/item_6_1.png',
		imageAlt: 'トマトコーポレーション 業務用スイートチリソース 750g',
		button: 'Amazonで商品を見る',
		note: 'セット数、原材料、賞味期限、保存方法、価格、在庫はAmazonの商品ページと届いた商品の表示をご確認ください。',
		tone: 'paper',
	},
	{
		id: 'toilet-paper',
		group: 'home',
		category: '暮らし・消耗品',
		channel: 'Amazon',
		title: '丸富製紙 ペンギン 超ロング再生紙5倍巻き',
		statement: '交換する回数と、置き場所を減らす。',
		lead: 'わが家のトイレットペーパーは5倍巻きです。交換回数が減り、ストックに必要なスペースも抑えられます。',
		details: [
			['使う場面', '毎日使うトイレットペーパーとして、家にストックしています。'],
			['良かった点', '1ロール250mの長巻きで、交換する回数と保管に必要なスペースを減らせます。'],
			['購入前の注意', 'シングル、芯なしの再生紙です。自宅のホルダーに収まるか、ケースの入数と保管場所を確認してください。'],
		],
		url: 'https://amzn.to/4wqFJpp',
		image: 'https://www.marutomi-seishi.co.jp/wp/wp-content/uploads/2020/11/5xr4rs-936x1024.jpg',
		imageAlt: '丸富製紙 ペンギン 超ロング再生紙5倍巻き 4ロール シングル',
		button: 'Amazonで商品を見る',
		note: '仕様、ケースの入数、価格、在庫、配送条件はAmazonの商品ページでご確認ください。',
		tone: 'olive',
	},
	{
		id: 'toy-cleaner',
		group: 'home',
		category: '家族・ごっこ遊び',
		channel: 'Amazon',
		title: 'アガツマ(AGATSUMA) アンパンマン スティックスイスイそうじき',
		statement: '掃除が、親子で同じことを楽しむ時間になる。',
		lead: '3歳の娘へのプレゼントとして購入しました。私が掃除機をかける横で、娘もアンパンマンのそうじきを持って一緒に掃除しています。',
		details: [
			['使う場面', '3歳の娘へのプレゼント。大人のまねをしながら遊んでいます。'],
			['良かった点', 'アンパンマンと一緒に遊びながら、自然に家事へ参加できます。'],
			['購入前の注意', 'メーカー対象年齢は3歳以上。電池不要で、使わないときは3つのパーツに分けて収納できます。'],
		],
		url: 'https://amzn.to/4gs6Qf5',
		image: 'https://m.media-amazon.com/images/I/71k5iP-aBdL._AC_SL1500_.jpg',
		imageAlt: 'アガツマ(AGATSUMA) アンパンマン スティックスイスイそうじき',
		button: 'Amazonで商品を見る',
		note: '対象年齢、セット内容、価格、在庫はAmazonの商品ページでご確認ください。',
		tone: 'coral',
	},
	{
		id: 'dyson-v8',
		group: 'home',
		category: '掃除・家電',
		channel: 'Amazon',
		title: 'Dyson V8 Slim Fluffy Extra',
		statement: '最新モデルでなくても、日々の掃除には十分。',
		lead: '新しい機種を追いかけるより、自分の使い方に必要な仕事をきちんとしてくれることを重視しています。',
		details: [
			['使う場面', '家の普段の掃除。娘がトイクリーナーを持って並ぶ時間にも使います。'],
			['良かった点', '取り回しやすく、日常の掃除に必要な仕事を十分にこなしてくれます。'],
			['購入前の注意', '型番によって付属品や販売条件が異なるため、セット内容を確認してください。'],
		],
		url: 'https://amzn.to/3U9FO3n',
		image: 'https://m.media-amazon.com/images/I/61pmiYdSkEL._AC_SL1500_.jpg',
		imageAlt: 'Dyson V8 Slim Fluffy Extra コードレス掃除機',
		button: 'Amazonで商品を見る',
		note: '型番、付属品、保証、価格、在庫はAmazonの商品ページでご確認ください。',
		tone: 'ink',
	},
	{
		id: 'dyson-stand',
		group: 'home',
		category: '掃除・収納',
		channel: 'Amazon',
		title: '八番屋 ダイソン対応スタンド',
		statement: '壁に穴を開けずに、置き場所をつくる。',
		lead: 'ダイソンの掃除機を置くために使っています。壁を傷つけたくない人には、本体と一緒に用意したいスタンドです。',
		details: [
			['使う場面', '壁へ収納用ブラケットを固定せず、掃除機の定位置をつくっています。'],
			['良かった点', '賃貸住宅や、壁へ穴を開けたくない部屋でも設置しやすいです。'],
			['購入前の注意', '手元のダイソンの型番が対応機種に含まれているか確認してください。'],
		],
		url: 'https://amzn.to/4xtkTXp',
		image: 'https://m.media-amazon.com/images/I/41llampoXkL._AC_SL1500_.jpg',
		imageAlt: '八番屋 ダイソン対応スタンド',
		button: 'Amazonで商品を見る',
		note: '対応機種、寸法、組み立て方法、設置条件、価格、在庫をご確認ください。',
		tone: 'olive',
	},
	{
		id: 'dishwasher',
		group: 'home',
		category: 'キッチン・家事',
		channel: 'Amazon',
		title: 'AVAKYO タンク式食器洗い乾燥機 AV-01',
		statement: '水道工事なしで、食器洗いを任せる場所をつくる。',
		lead: 'タンクへ給水して使える食洗機です。水道の分岐工事が難しい場所でも設置を考えやすく、毎日の洗い物を任せられます。',
		details: [
			['使う場面', '毎日の食器洗い。電源、給水、排水を確保できる場所へ置いています。'],
			['良かった点', '水道工事をせずに導入でき、手洗いに使う時間を減らせます。'],
			['購入前の注意', '本体寸法、電源、給水方法、排水ホースを安全に固定できる場所が必要です。'],
		],
		url: 'https://amzn.to/4qijmBm',
		image: 'https://m.media-amazon.com/images/I/51hQDbCYBOL._AC_SL1500_.jpg',
		imageAlt: 'AVAKYO タンク式食器洗い乾燥機 AV-01 ホワイト',
		button: 'Amazonで商品を見る',
		note: '設置寸法、給排水方法、電源条件、容量、対応食器、洗剤、価格、在庫をご確認ください。',
		tone: 'paper',
	},
	{
		id: 'hotcook',
		group: 'home',
		category: 'キッチン・家事',
		channel: 'Amazon',
		title: 'シャープ ヘルシオ ホットクック KN-HW24G-R',
		statement: '朝10分で、夕飯の仕込みを終わらせる。',
		lead: '朝に材料と調味料を入れておけば、帰宅する頃には夕飯ができています。安くはありませんが、子育て世代には必須級の調理家電です。',
		details: [
			['使う場面', '朝の10分ほどで材料と調味料を入れ、夕飯の調理を任せています。'],
			['良かった点', '帰宅後に一から夕飯を作る負担が減り、子どもと過ごす時間を確保できます。'],
			['購入前の注意', 'リンク先はレッドの2.4Lタイプで、量の目安は2〜6人分です。本体寸法、ふたを開く空間、蒸気の逃げ道、お手入れ方法を確認してください。'],
		],
		url: 'https://amzn.to/4qfDzYe',
		image: 'https://jp.sharp/hotcook/products/knhw24g/images/gallery-1.jpg',
		imageAlt: 'シャープ ヘルシオ ホットクック KN-HW24G-R レッドの正面',
		button: 'Amazonで商品を見る',
		note: '型番、カラー、容量、付属品、保証、価格、在庫はAmazonの商品ページでご確認ください。',
		tone: 'coral',
		featured: true,
	},
	{
		id: 'dryer',
		group: 'home',
		category: '洗濯・家事',
		channel: 'Amazon',
		title: '日立 電気衣類乾燥機 DE-N50HV',
		statement: '子育て世代の洗濯には、必須級。',
		lead: '洗濯した衣類をそのまま乾燥まで進められることが助かっています。干す作業と、天気を待つ時間を減らせます。',
		details: [
			['使う場面', '子どもがいて洗濯物が多い日の、毎日の乾燥。メーカー仕様の乾燥容量は5kgです。'],
			['良かった点', '洗濯後の家事をひとつ減らし、子どもと過ごす時間やほかの家事に時間を使えます。'],
			['購入前の注意', '本体寸法、搬入経路、設置台、周囲の空間、電源条件を確認してください。'],
		],
		url: 'https://www.amazon.co.jp/dp/B0BJPMQN8P?tag=tamashirotool-22',
		image: 'https://m.media-amazon.com/images/I/51Itmsso+sL._AC_SL1000_.jpg',
		imageAlt: '日立 電気衣類乾燥機 DE-N50HV ピュアホワイト',
		button: 'Amazonで商品を見る',
		note: '乾燥容量、設置条件、別売品、搬入、電源、使用できる衣類、価格、在庫をご確認ください。',
		tone: 'coral',
		featured: true,
	},
];

const collectionDefinitions = [
	{
		id: 'work-tools',
		group: 'work',
		label: 'まず見てほしい2品',
		title: '仕事の道具',
		intro: '相談や会議の記録、外出先での通信に使っています。仕事の手間を減らし、目の前の相手に集中するための道具です。',
		href: '/toolbox/work/',
		linkLabel: '仕事の道具を詳しく見る',
	},
	{
		id: 'home-tools',
		group: 'home',
		label: 'まず見てほしい3品',
		title: '家の道具',
		intro: '鍵、掃除、食器、洗濯、飲み物、子どもとの時間。家事の負担を減らし、家族と過ごす時間をつくるための道具です。',
		href: '/toolbox/home/',
		linkLabel: '家の道具をすべて見る',
	},
];

const selectedIds = {
	work: ['hidock-p1', 'nihontsushin-sim'],
	home: ['hotcook', 'dishwasher', 'switchbot-lock'],
};

const visibleDefinitions = view === 'index'
	? collectionDefinitions
	: collectionDefinitions.filter((collection) => collection.group === view);

const toolCollections = visibleDefinitions.map((collection) => {
	const allItems = tools.filter((tool) => tool.group === collection.group);
	return {
		...collection,
		label: view === 'index' ? collection.label : `${allItems.length}品を掲載`,
		items: view === 'index'
			? allItems.filter((tool) => selectedIds[collection.group as keyof typeof selectedIds].includes(tool.id))
			: allItems,
		count: allItems.length,
	};
});

const pageCopy = {
	index: {
		title: '玉城祐輔の道具箱｜仕事用2品と家庭用3品を厳選',
		description: '玉城祐輔が実際に使っている道具から、仕事用2品と家庭用3品を厳選。ほかの道具は分野ごとの一覧で紹介します。',
		eyebrow: '玉城祐輔の道具箱',
		lead: '仕事用2品と家庭用3品を厳選しました。ほかの道具は、分野ごとのページで見られます。',
	},
	work: {
		title: '仕事で使っている道具｜玉城祐輔の道具箱',
		description: '相談、会議、外出先で玉城祐輔が実際に使っている仕事の道具を、使い方と注意点まで紹介します。',
		eyebrow: '仕事で使っている道具',
		lead: '相談の記録と外出先の通信。仕事の手間を減らすために使っている道具を、詳しく紹介します。',
	},
	home: {
		title: '家で使っている道具｜玉城祐輔の道具箱',
		description: '家事や子育てで玉城祐輔が実際に使っている家庭用の道具を、使い方と注意点まで紹介します。',
		eyebrow: '家で使っている道具',
		lead: '家事の回数や待つ時間を減らすために、家で使っている道具をまとめました。',
	},
}[view];

---

<!doctype html>
<html lang="ja">
	<head>
		<BaseHead
			title={pageCopy.title}
			description={pageCopy.description}
		/>
	</head>
	<body class:list={['toolbox-page', `toolbox-${view}`]}>
		<Header />
		<main id="main-content">
			<section class="toolbox-hero shell">
				<div class="hero-copy">
					{view !== 'index' && <a class="back-link" href="/toolbox/">道具箱の入口へ戻る</a>}
					<p class="eyebrow">{pageCopy.eyebrow}</p>
					{view === 'index' && <h1>まずは、<strong>この5品から。</strong></h1>}
					{view === 'work' && <h1>仕事で使う、<strong>2つの道具。</strong></h1>}
					{view === 'home' && <h1>家事を減らす、<strong>11の道具。</strong></h1>}
					<p class="hero-lead">{pageCopy.lead}</p>
					<div class="button-row">
						{view === 'index' && <a class="button primary" href="#work-tools">仕事用2品を見る</a>}
						{view === 'index' && <a class="button" href="#home-tools">家庭用3品を見る</a>}
						{view === 'work' && <a class="button primary" href="#work-tools">2品を見る</a>}
						{view === 'home' && <a class="button primary" href="#home-tools">11品を見る</a>}
					</div>
				</div>
			</section>

			<section class="tools shell" id="tools" aria-label="紹介している道具">
				{toolCollections.map((collection) => (
					<section id={collection.id} class="tool-collection" aria-labelledby={`${collection.id}-title`}>
						<header class="collection-heading">
							<p>{collection.label}</p>
							<h2 id={`${collection.id}-title`}>{collection.title}</h2>
							<span>{collection.intro}</span>
						</header>
						<div class="tool-grid">
							{collection.items.map((tool) => (
								<article id={tool.id} class:list={['tool-card', tool.tone, { featured: view === 'index' ? tool.id === 'hotcook' : tool.featured, wide: view !== 'index' && tool.id === 'toy-cleaner' }]}>
									<a class="tool-image" href={tool.url} target="_blank" rel="sponsored noopener noreferrer" aria-label={`${tool.title}の商品ページを開く`}>
										<img src={tool.image} alt={tool.imageAlt} loading="lazy" decoding="async" referrerpolicy="no-referrer" />
									</a>
									<div class="tool-copy">
										<div class="tool-meta"><span>{tool.category}</span><span>{tool.channel}</span></div>
										<h3>{tool.title}</h3>
										<p class="tool-statement">{tool.statement}</p>
										<p class="tool-lead">{tool.lead}</p>
										<a class="product-link" href={tool.url} target="_blank" rel="sponsored noopener noreferrer">{tool.button}<span aria-hidden="true">↗</span></a>
										{'officialUrl' in tool && <a class="official-link" href={tool.officialUrl} target="_blank" rel="noopener noreferrer">公式の料金・条件を見る<span aria-hidden="true">↗</span></a>}
										<p class="affiliate-note">{tool.note}</p>
									<details class="tool-notes" open={view !== 'index'}>
											<summary>使い方と注意点</summary>
											<dl>
												{tool.details.map(([label, value]) => (
													<div><dt>{label}</dt><dd>{value}</dd></div>
												))}
											</dl>
										</details>
									</div>
								</article>
							))}
						</div>
						{view === 'index' && (
							<a class="collection-action" href={collection.href}>
								<span>{collection.linkLabel}</span>
								<strong>{collection.count}品</strong>
							</a>
						)}
					</section>
				))}
			</section>

			<section class="closing shell">
				<p>道具を増やすためではなく、目の前の人と仕事に集中するために選びます。</p>
				<div>
					<strong>玉城祐輔</strong>
					<a href="/affiliate-disclosure/">広告・紹介について</a>
				</div>
			</section>

			<aside class="disclosure" aria-label="広告に関する表示">
				<div class="shell disclosure-inner">
					<strong>広告</strong>
					<p>このページにはアフィリエイトリンクが含まれます。Amazonのアソシエイトとして、玉城祐輔は適格販売により収入を得ています。</p>
				</div>
			</aside>
		</main>
		<Footer />
	</body>
</html>

<style>
	.toolbox-hero {
		display: grid;
		grid-template-columns: minmax(0, 1.05fr) minmax(340px, 0.75fr);
		align-items: center;
		gap: clamp(3rem, 8vw, 8rem);
		padding-block: clamp(4rem, 9vw, 8rem);
	}

	.hero-copy h1 {
		max-width: 800px;
		margin-bottom: 1.6rem;
		font-size: clamp(3rem, 7vw, 6.8rem);
		line-height: 1.05;
	}

	.hero-copy h1 strong {
		display: inline-block;
		font-weight: 750;
	}

	.hero-lead {
		max-width: 620px;
		margin-bottom: 2rem;
		color: var(--ink-soft);
		font-size: clamp(1rem, 1.7vw, 1.2rem);
	}

	.disclosure {
		border-block: 1px solid var(--line);
		background: var(--ink);
		color: var(--paper);
	}

	.disclosure-inner {
		display: grid;
		grid-template-columns: 220px minmax(0, 760px);
		gap: 2rem;
		padding-block: 1.5rem;
	}

	.disclosure strong { color: var(--coral); font-size: 0.8rem; }
	.disclosure p { margin: 0; color: rgba(255, 253, 248, 0.76); font-size: 0.78rem; line-height: 1.8; }

	.tools { padding-block: clamp(5rem, 10vw, 9rem); }

	.tool-index {
		margin-bottom: clamp(2.5rem, 6vw, 5rem);
		padding: 1.25rem 0;
		border-block: 1px solid var(--line);
	}

	.tool-index > p {
		margin-bottom: 1rem;
		font-size: 0.75rem;
		font-weight: 800;
	}

	.tool-index ol {
		display: grid;
		grid-template-columns: repeat(3, minmax(0, 1fr));
		gap: 0;
		margin: 0;
		padding: 0;
		list-style: none;
	}

	.tool-index li {
		min-width: 0;
		border-top: 1px solid var(--line);
	}

	.tool-index a {
		display: grid;
		gap: 0.1rem 0.5rem;
		padding: 0.85rem 0.5rem;
		font-size: 0.78rem;
		font-weight: 800;
		line-height: 1.5;
		text-decoration: none;
	}

	.tool-index a span {
		color: var(--muted);
		font-size: 0.65rem;
		font-weight: 700;
	}

	.tool-list { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 1.25rem; }

	.tool-card {
		display: grid;
		grid-template-columns: minmax(0, 1fr);
		min-width: 0;
		gap: clamp(1.5rem, 4vw, 4rem);
		padding: clamp(1.8rem, 4vw, 3.5rem);
		border: 1px solid var(--line);
		border-radius: var(--radius);
		background: var(--white);
	}

	.tool-card.featured {
		grid-column: 1 / -1;
		grid-template-columns: minmax(0, 0.92fr) minmax(360px, 1.08fr);
		padding-block: clamp(3rem, 6vw, 5.5rem);
	}
	.tool-card.coral { background: linear-gradient(135deg, rgba(227, 111, 74, 0.13), transparent 52%), var(--white); }
	.tool-card.olive { background: linear-gradient(145deg, rgba(102, 114, 75, 0.15), transparent 58%), var(--white); }
	.tool-card.ink { background: var(--ink); color: var(--paper); }

	.tool-image {
		position: relative;
		grid-column: 1 / -1;
		min-height: 280px;
		overflow: hidden;
		border: 1px solid var(--line);
		border-radius: calc(var(--radius) - 0.35rem);
		background: #fff;
		text-decoration: none;
	}

	.tool-image img {
		width: 100%;
		height: clamp(280px, 34vw, 430px);
		padding: clamp(1.5rem, 4vw, 3.25rem);
		object-fit: contain;
		transition: transform 220ms ease;
	}

	.tool-image:hover img { transform: scale(1.025); }

	.tool-meta {
		display: flex;
		flex-wrap: wrap;
		gap: 0.5rem 1rem;
		margin-bottom: 1rem;
		color: var(--muted);
		font-size: 0.7rem;
		font-weight: 800;
		letter-spacing: 0.06em;
	}

	.tool-meta span + span::before { margin-right: 1rem; color: var(--line); content: '/'; }

	.tool-main h3 {
		margin-bottom: 1.3rem;
		font-size: clamp(1.8rem, 3.8vw, 3.7rem);
	}

	.tool-card:not(.featured) .tool-main h3 {
		font-size: clamp(1.65rem, 2.8vw, 2.45rem);
	}

	.tool-statement {
		margin-bottom: 1rem;
		color: var(--olive-dark);
		font-family: var(--font-serif);
		font-size: clamp(1.35rem, 2.4vw, 2.2rem);
		font-weight: 700;
		line-height: 1.5;
	}

	.tool-lead { margin: 0; color: var(--muted); }

	.tool-detail dl { display: grid; gap: 1.25rem; margin: 0; }

	.tool-detail dl > div {
		display: grid;
		grid-template-columns: 7rem 1fr;
		gap: 1rem;
		padding-top: 1.1rem;
		border-top: 1px solid var(--line);
	}

	.tool-detail dt { color: var(--coral); font-size: 0.72rem; font-weight: 850; }
	.tool-detail dd { margin: 0; color: var(--ink-soft); font-size: 0.86rem; line-height: 1.75; }

	.product-link {
		display: inline-flex;
		align-items: center;
		gap: 0.7rem;
		margin-top: 2rem;
		padding-bottom: 0.35rem;
		border-bottom: 2px solid currentColor;
		font-weight: 850;
		text-decoration: none;
	}

	.product-link span { color: var(--coral); }
	.official-link {
		display: inline-flex;
		align-items: center;
		align-self: flex-start;
		gap: 0.55rem;
		margin-top: 0.85rem;
		color: var(--ink-soft);
		font-size: 0.76rem;
		font-weight: 800;
		text-underline-offset: 0.25rem;
	}
	.official-link span { color: var(--coral); }
	.affiliate-note { margin: 0.8rem 0 0; color: var(--muted); font-size: 0.68rem; line-height: 1.65; }

	.tool-card.ink .tool-meta,
	.tool-card.ink .tool-lead,
	.tool-card.ink .tool-detail dd,
	.tool-card.ink .affiliate-note { color: rgba(244, 240, 231, 0.7); }
	.tool-card.ink .tool-statement,
	.tool-card.ink .product-link { color: var(--paper); }
	.tool-card.ink .tool-detail dl > div { border-color: rgba(244, 240, 231, 0.2); }

	.closing {
		display: flex;
		align-items: end;
		justify-content: space-between;
		gap: 2rem;
		padding-block: clamp(4rem, 9vw, 8rem);
		border-top: 1px solid var(--line);
	}

	.closing > p {
		max-width: 850px;
		margin: 0;
		font-family: var(--font-serif);
		font-size: clamp(2rem, 4.5vw, 4.5rem);
		font-weight: 700;
		line-height: 1.45;
	}

	.closing > div { display: grid; gap: 0.6rem; min-width: 12rem; }
	.closing a { font-size: 0.78rem; }

	@media (max-width: 940px) {
		.toolbox-hero { grid-template-columns: 1fr; }

		.tool-list { grid-template-columns: 1fr; }
		.tool-card,
		.tool-card.featured { grid-column: auto; grid-template-columns: minmax(0, 1fr); }
		.tool-image,
		.tool-detail { grid-column: auto; }
		.tool-card > * { min-width: 0; }
	}

	@media (max-width: 640px) {
		.toolbox-hero { padding-block: 3.5rem 5rem; }
		.hero-copy h1 { font-size: clamp(2.7rem, 13vw, 4.5rem); }
		.disclosure-inner { grid-template-columns: 1fr; gap: 0.5rem; }
		.tool-index ol { grid-template-columns: 1fr; }
		.tool-card { grid-template-columns: 1fr; padding: 1.5rem; }
		.tool-image { grid-column: auto; min-height: 220px; }
		.tool-image img { height: 260px; padding: 1.25rem; }
		.tool-detail { grid-column: auto; }
		.tool-detail dl > div { grid-template-columns: 1fr; gap: 0.3rem; }
		.closing { align-items: start; flex-direction: column; }
	}

	/* Toolbox redesign: grouped editorial catalog */
	:global(body.toolbox-page) {
		background: var(--paper);
	}

	.toolbox-hero {
		grid-template-columns: minmax(0, 1fr);
		gap: clamp(2.5rem, 6vw, 6rem);
		min-height: clamp(28rem, 56dvh, 36rem);
		padding-block: clamp(4rem, 7vw, 6rem);
	}

	.hero-copy h1 {
		max-width: none;
		font-size: clamp(3rem, 4vw, 3.75rem);
		line-height: 1.08;
	}

	.hero-copy h1 strong {
		font-weight: 750;
	}

	.hero-lead {
		max-width: 64rem;
		font-size: clamp(1rem, 1.35vw, 1.12rem);
	}

	.back-link {
		display: inline-block;
		margin-bottom: 1.4rem;
		color: var(--muted);
		font-size: 0.78rem;
		font-weight: 750;
		text-underline-offset: 0.3rem;
	}

	.disclosure {
		border-block: 1px solid var(--line);
		background: var(--paper-deep);
		color: var(--ink);
	}

	.disclosure strong {
		color: var(--coral);
	}

	.disclosure p {
		color: var(--ink-soft);
	}

	.tools {
		padding-block: clamp(5rem, 9vw, 8rem);
	}

	.tool-collection {
		scroll-margin-top: 6rem;
		padding-top: clamp(3.5rem, 7vw, 6rem);
		border-top: 1px solid var(--line);
	}

	.tool-collection:first-child {
		padding-top: 0;
		border-top: 0;
	}

	.tool-collection + .tool-collection {
		margin-top: clamp(5rem, 9vw, 8rem);
	}

	.collection-heading {
		max-width: 760px;
		margin-bottom: clamp(2rem, 5vw, 4rem);
	}

	.collection-heading p {
		margin-bottom: 0.8rem;
		color: var(--coral);
		font-size: 0.75rem;
		font-weight: 850;
	}

	.collection-heading h2 {
		margin-bottom: 1rem;
		font-size: clamp(2rem, 4.3vw, 4rem);
	}

	.collection-heading > span {
		display: block;
		max-width: 42rem;
		color: var(--muted);
	}

	.tool-grid {
		display: grid;
		grid-template-columns: repeat(12, minmax(0, 1fr));
		gap: 1.25rem;
	}

	.collection-action {
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 1rem;
		margin-top: 1.25rem;
		padding: 1.3rem 1.5rem;
		border: 1px solid var(--ink);
		border-radius: var(--radius);
		font-weight: 850;
		text-decoration: none;
		transition: transform 160ms ease, background 160ms ease, color 160ms ease;
	}

	.collection-action:hover {
		transform: translateY(-2px);
		background: var(--ink);
		color: var(--white);
	}

	.collection-action:active {
		transform: translateY(-1px) scale(0.99);
	}

	.collection-action strong {
		flex: 0 0 auto;
		color: var(--coral);
		font-size: 0.78rem;
	}

	.tool-card {
		grid-column: span 6;
		grid-template-columns: 1fr;
		align-content: start;
		gap: 1.35rem;
		padding: clamp(1.2rem, 2.5vw, 2rem);
		border-radius: var(--radius);
		background: var(--white);
	}

	.tool-card.featured,
	.tool-card.wide {
		grid-column: 1 / -1;
		grid-template-columns: minmax(240px, 0.72fr) minmax(0, 1.28fr);
		align-items: start;
		padding: clamp(1.4rem, 3vw, 2.5rem);
	}

	.tool-card.coral {
		background: color-mix(in srgb, var(--coral) 9%, var(--white));
	}

	.tool-card.olive {
		background: color-mix(in srgb, var(--olive) 12%, var(--white));
	}

	.tool-card.ink {
		background: #1b1e18;
		color: #f4f0e7;
	}

	.tool-card.ink .tool-statement,
	.tool-card.ink .product-link {
		color: #f4f0e7;
	}

	.tool-image {
		grid-column: auto;
		min-height: 0;
		border-radius: calc(var(--radius) - 0.3rem);
	}

	.tool-image img {
		height: clamp(220px, 25vw, 320px);
		padding: clamp(1.2rem, 3vw, 2.4rem);
	}

	.tool-card.featured .tool-image img,
	.tool-card.wide .tool-image img {
		height: clamp(260px, 25vw, 360px);
		min-height: 0;
		padding: clamp(1.25rem, 2.5vw, 2rem);
	}

	.tool-copy {
		display: flex;
		min-width: 0;
		flex-direction: column;
	}

	.tool-copy h3 {
		margin-bottom: 1rem;
		font-size: clamp(1.55rem, 2.5vw, 2.7rem);
	}

	.tool-card.featured .tool-copy h3,
	.tool-card.wide .tool-copy h3 {
		font-size: clamp(1.9rem, 3.4vw, 3.5rem);
	}

	.tool-statement {
		font-size: clamp(1.2rem, 1.9vw, 1.75rem);
	}

	.tool-lead {
		font-size: 0.9rem;
	}

	.product-link {
		align-self: flex-start;
		margin-top: 1.5rem;
		font-size: 0.86rem;
	}

	.affiliate-note {
		margin-bottom: 1rem;
	}

	.tool-notes {
		margin-top: auto;
		padding-top: 0.9rem;
		border-top: 1px solid var(--line);
	}

	.tool-notes summary {
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 1rem;
		min-height: 2.5rem;
		cursor: pointer;
		font-size: 0.78rem;
		font-weight: 850;
		list-style: none;
	}

	.tool-notes summary::-webkit-details-marker {
		display: none;
	}

	.tool-notes summary::after {
		color: var(--coral);
		content: "+";
		font-size: 1.1rem;
	}

	.tool-notes[open] summary::after {
		content: "−";
	}

	.tool-notes dl {
		display: grid;
		gap: 0;
		margin: 0.5rem 0 0;
	}

	.tool-notes dl > div {
		display: grid;
		grid-template-columns: 6.5rem 1fr;
		gap: 1rem;
		padding: 0.9rem 0;
		border-top: 1px solid var(--line);
	}

	.tool-notes dt {
		color: var(--coral);
		font-size: 0.7rem;
		font-weight: 850;
	}

	.tool-notes dd {
		margin: 0;
		color: var(--ink-soft);
		font-size: 0.78rem;
		line-height: 1.7;
	}

	.tool-card.ink .tool-notes dd {
		color: rgba(244, 240, 231, 0.72);
	}

	@media (max-width: 940px) {
		.toolbox-hero {
			grid-template-columns: 1fr;
			min-height: 0;
		}

		.tool-card,
		.tool-card.featured,
		.tool-card.wide {
			grid-column: 1 / -1;
		}
	}

	@media (max-width: 680px) {
		.toolbox-hero {
			padding-block: 3rem 4rem;
		}

		.hero-copy h1 {
			font-size: clamp(2.45rem, 11.5vw, 3.35rem);
		}

		.disclosure-inner {
			grid-template-columns: 1fr;
		}

		.tool-card.featured,
		.tool-card.wide {
			grid-template-columns: 1fr;
		}

		.tool-card.featured .tool-image img,
		.tool-card.wide .tool-image img {
			height: 260px;
			min-height: 0;
		}

		.tool-notes dl > div {
			grid-template-columns: 1fr;
			gap: 0.25rem;
		}

		.collection-action {
			padding: 1.1rem 1.2rem;
		}

	}

	@media (prefers-reduced-motion: reduce) {
		.tool-image img,
		.collection-action {
			transition: none;
		}
	}

	@media (prefers-color-scheme: dark) {
		:global(body.toolbox-page) {
			--ink: #f0f2eb;
			--ink-soft: #d0d4c8;
			--paper: #151712;
			--paper-deep: #22251e;
			--white: #1b1e18;
			--olive: #8e9a70;
			--olive-dark: #c0caa9;
			--coral: #f08a64;
			--line: rgba(240, 242, 235, 0.17);
			--muted: #a9ad9f;
			--shadow: 0 24px 70px rgba(0, 0, 0, 0.32);
		}

		:global(body.toolbox-page footer) {
			--ink: #0f110e;
			--paper: #eceee6;
			--white: #eceee6;
			--coral: #f08a64;
		}

		.tool-card.ink {
			background: #0f110e;
		}
	}
</style>
